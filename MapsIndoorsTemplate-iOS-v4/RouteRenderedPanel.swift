import SwiftUI

struct RouteRenderedPanel: View {
    var body: some View {
        VStack {
            Spacer()
            Text("Route Rendered Successfully!")
        }
        .padding()
        .background(Color.white)
        .cornerRadius(20)
    }
}
